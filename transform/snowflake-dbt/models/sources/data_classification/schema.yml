version: 2

models:
  - name: sensitive_queries_sources
    description: '{{ doc("sensitive_queries_sources") }}'
    columns:
      - name: TBA
        data_tests:
          - not_null
          - unique