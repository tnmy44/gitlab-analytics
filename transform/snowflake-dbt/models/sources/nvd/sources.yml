version: 2

sources:
  - name: nvd
    database: '{{ env_var("SNOWFLAKE_STATIC_DATABASE") }}'
    schema: sensitive

    quoting:
      database: true
      schema: false
      identifier: false

    tables:
    - name: nvd_cve