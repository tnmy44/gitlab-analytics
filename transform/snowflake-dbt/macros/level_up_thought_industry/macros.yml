version: 2

macros:
  - name: level_up_incremental
    description: '{{ doc("level_up_incremental") }}'
    arguments:
      - name: source_table
        type: string
        description: Name of the source table (from schema.yml file).
      - name: source_schema
        type: string
        description: Name of the source table (from schema.yml file). Default value is level_up.
