connection_info:
  pass: ZUORA_API_PASS
  user: ZUORA_API_USER
tables:
  ramp:
    query: |-
      SELECT
        *
      FROM Ramp
  rampinterval:
    query: |-
      SELECT
        *
      FROM RampInterval